machine:
  services:
    - docker

checkout:
  pre:
    - echo "set -v" >> ~/.bash_profile

dependencies:
  override:
    - docker info
    - docker pull alpine:latest

test:
  override:
    - docker run alpine:latest apk update
