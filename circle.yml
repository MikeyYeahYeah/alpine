machine:
  services:
    - docker

checkout:
  pre:
    - history

dependencies:
  override:
    - docker info
    - docker pull alpine:latest

test:
  override:
    - docker run alpine:latest apk update
